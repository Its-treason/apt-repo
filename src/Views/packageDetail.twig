<!DOCTYPE html>
<html lang="en">
<head>
    <title>Package - {{ package.filename }}</title>
</head>
<body>
<header>
    <h1>Package: "{{ package.filename }}"</h1>
</header>
<main>
    <table>
        <tbody>
        <tr>
            <td>Name</td>
            <td>{{ package.name }}</td>
        </tr>
        <tr>
            <td>Version</td>
            <td>{{ package.version }}</td>
        </tr>
        <tr>
            <td>Arch</td>
            <td>{{ package.arch }}</td>
        </tr>
        <tr>
            <td>Internal Id</td>
            <td>{{ package.id }}</td>
        </tr>
        <tr>
            <td>Upload date</td>
            <td>{{ package.uploadDate|date('Y-m-d H:i:s') }}</td>
        </tr>
        </tbody>
    </table>
    <hr/>
    <h3>Add suite</h3>
    {% if availableSuites %}
        <form method="post" action="/ui/packages/{{ package.filename }}/suites" enctype="multipart/form-data">
            <label>
                Select suite
                <select name="suite">
                    {% for suite in availableSuites %}
                        <option value="{{ suite.codename }}-{{ suite.suite }}">{{ suite.codename }}
                            - {{ suite.suite }}</option>
                    {% endfor %}
                </select>
            </label>
            <input type="submit" value="Add"/>
        </form>
    {% else %}
        <i>No suites available</i>
    {% endif %}
    <h3>All suite</h3>
    {% if not currentSuites %}
        <i>Packages in no suites</i>
    {% endif %}
    <ul>
        {% for suite in currentSuites %}
            <li>{{ suite.codename }} - {{ suite.suite }}</li>
        {% endfor %}
    </ul>
</main>
<footer>
    <hr/>
    <a href="/ui/packages">Back to package list</a>
</footer>
</body>
</html>
